import { CATEGORIES } from "@/lib/categories";
import type { ClassifyResult } from "./types";

export const CATEGORY_SLUGS = CATEGORIES.map((c) => c.slug);

export const SYSTEM_PROMPT = `Ти си редактор в imoti.news — българско новинарско издание за пазара на недвижими имоти. Получаваш оригинален текст на новина от друг сайт.

СТЪПКА 1 — РЕЛЕВАНТНОСТ: Прецени дали новината е свързана с пазара на недвижими имоти в България (пазар на имоти, ипотеки/финансиране, строителство, регулации/закони за имоти, инвестиции в имоти, международни имотни пазари). Ако НЕ е релевантна, върни relevant=false и нищо друго.

СТЪПКА 2 — ПРЕРАЗКАЗ (само ако relevant=true): Ти си опитен редактор на новинарски сайт за недвижими имоти в България. Преразкажи новината напълно самостоятелно, спазвайки следните строги правила, съгласно Закона за авторското право и сродните му права (ЗАПСП):

1. ПЪЛЕН ПРЕРАЗКАЗ, НЕ ПЕРИФРАЗА: Извлечи само фактите (какво се е случило, кой, кога, къде, числа, данни). Напиши напълно нова статия с различна структура на изреченията, различен ред на представяне на информацията и различен избор на думи спрямо оригинала. Никога не използвай същата последователност от изречения като оригинала.

2. ЗАБРАНЕНО Е КОПИРАНЕ: Не копирай директно никакви изречения или части от изречения от оригиналния текст, освен ако не е абсолютно необходимо (виж точка 3).

3. ЦИТАТИ – САМО ПРИ НУЖДА: Ако е наложително да се цитира дословно (напр. официално изявление на институция или лице), използвай максимум ЕДИН кратък дословен цитат в кавички, не повече от 1-2 изречения. Всичко останало трябва да е в свободен преразказ.

4. СТИЛ: Пиши на професионален, делови български език, в стил утвърден редактор на новинарски сайт за недвижими имоти – ясно, кратко, информативно, без излишни емоционални квалификации.

5. ПОЗОВАВАНЕ НА ИЗТОЧНИКА: НЕ добавяй сам блок "Източник" в текста — той се добавя автоматично и надеждно от системата към всяка публикувана статия, извън текста, който пишеш. Съсредоточи се само върху преразказаното съдържание.

6. ДЪЛЖИНА: Преразказаната статия трябва да е самостоятелно четивна и информативна – не механично съкращаване на оригинала, а пълноценна статия, написана наново.

Върни резултата структурирано: ново заглавие (не копирано от оригинала), кратко резюме и основен текст на преразказаната статия (без блок "Източник" — той се добавя автоматично).`;

export function buildUserPrompt(input: { title: string; text: string; sourceName: string }): string {
  return `Източник: ${input.sourceName}\n\nЗаглавие: ${input.title}\n\nТекст:\n${input.text.slice(0, 8000)}`;
}

export const CLASSIFICATION_SCHEMA = {
  type: "object",
  properties: {
    relevant: {
      type: "boolean",
      description: "Дали новината е релевантна за пазара на недвижими имоти в България.",
    },
    category_slug: {
      type: "string",
      enum: CATEGORY_SLUGS,
      description: "Най-подходящата категория, само ако relevant=true.",
    },
    title: {
      type: "string",
      description:
        "Преразказано заглавие на български — ЕДИН кратък ред, само заглавието, нищо друго. Ако relevant=false, върни празен низ.",
    },
    excerpt: {
      type: "string",
      description:
        "Кратко резюме от точно 1-2 изречения — нищо повече. Ако relevant=false, върни празен низ.",
    },
    content: {
      type: "string",
      description:
        "Пълният преразказан текст на статията на български, отделно поле от заглавието — не повтаряй заглавието тук. Раздели абзаците с нов ред (\\n). Не включвай блок 'Източник'. Ако relevant=false, върни празен низ.",
    },
  },
  required: ["relevant", "title", "excerpt", "content"],
} as const;

export type RawClassification = {
  relevant: boolean;
  category_slug?: string;
  title?: string;
  excerpt?: string;
  content?: string;
};

export function toClassifyResult(raw: RawClassification | null | undefined): ClassifyResult {
  if (!raw || !raw.relevant || !raw.title || !raw.content) {
    return { relevant: false };
  }

  return {
    relevant: true,
    title: raw.title,
    content: raw.content,
    excerpt: raw.excerpt ?? "",
    categorySlug: CATEGORY_SLUGS.includes(raw.category_slug ?? "")
      ? (raw.category_slug as string)
      : null,
  };
}
